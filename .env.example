# Neon Postgres connection string (only needed for Phase 2+ DB ingestion).
# Phase 1 renders the tier list from seed/vehicles.json and needs NO database.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
